import React from "react";
import "./Header.scss";

const Header = () => (
    <div className="header">
        <span className="header__title">
            UUID Generator
        </span>
    </div>
);

export default Header;
